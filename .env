# Add environment variables for this project here.

# Node API + PostgreSQL (local defaults)
API_PORT=4000
PGHOST=127.0.0.1
PGPORT=5432
PGUSER=ajeet
PGDATABASE=ednovate_db
# PGPASSWORD=
# DATABASE_URL=postgresql://ajeet@localhost:5432/ednovate_db
